version: 0.2

phases:
  install:
    commands:
      - echo Entrando en la fase de instalacion...
      - npm install -g mocha@2.3.1
      - echo Saliendo de la fase de instalacion...
  pre_build:
    commands:
      - echo Entrando en la fase de pre_build...
      - npm install
      - echo Saliendo de la fase de pre_build...
  build:
    commands:
      - echo Entrando en la fase de build...
      - npm test
      - echo Saliendo de la fase de build...
